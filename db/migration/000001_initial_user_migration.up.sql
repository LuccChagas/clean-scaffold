CREATE TABLE users (
                       id UUID PRIMARY KEY,
                       age INTEGER,
                       name VARCHAR(255),
                       birth_date TIMESTAMP,
                       status BOOLEAN
);